import { Link } from "react-router-dom";

const PersonDetails = ({ location, id }) => {
  //   <Link state={{ from: location }} to={`/person/${id}`}>
  //   </Link>;
};

export default PersonDetails;
